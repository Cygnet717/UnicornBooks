<div class="card col-3 m-3" style="width: 18rem;">
  <div class="card-body d-flex justify-content-around flex-column">
    <h5 class="card-title text-center">{{book_name}}</h5>
    <p class="card-text">{{description}}</p>
    {{#if user_id}}
    
    <a href= "#" class="card-link text-light bg-danger p-2 text-center">Return Book</a>
    {{else}}
    <a href="#" class="card-link bg-success text-white p-2 text-center">Available</a>
    {{/if}}
  </div>
</div>